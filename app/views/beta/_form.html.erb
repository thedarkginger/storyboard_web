<br> 

<%= form_for(@betum) do |f| %>
  <% if @betum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@betum.errors.count, "error") %> prohibited this betum from being saved:</h2>

      <ul>
      <% @betum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Name of Contact" %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label "How many listeners do you expect?" %><br>
    <%= f.text_field :expected_number_listeners, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Apply for Beta", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
